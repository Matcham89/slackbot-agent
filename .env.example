# Slack Tokens - Get these from https://api.slack.com/apps
SLACK_BOT_TOKEN=xoxb-your-bot-token
SLACK_APP_TOKEN=xapp-your-app-token

# ============================================================================
# Kagent Configuration
# ============================================================================

# New to Kagent? Follow: https://kagent.dev/docs/kagent/getting-started/quickstart
# The quickstart creates a kind cluster with Kagent in the "kagent" namespace
# and sets up the default "k8s-agent"

# Option 1: Single URL (recommended for local development)
# Format: http://<host>:<port>/api/a2a/<namespace>/<agent-name>
# NOTE: No trailing slash (will auto-redirect)

# For local development with port-forward:
KAGENT_A2A_URL=http://localhost:8083/api/a2a/kagent/k8s-agent

# For in-cluster deployment:
# KAGENT_A2A_URL=http://kagent-controller.kagent.svc.cluster.local:8083/api/a2a/kagent/k8s-agent

# ============================================================================
# Option 2: Separate components (recommended for Kubernetes deployments)
# Use these if you want to pass namespace and agent name as separate env vars
# Uncomment and comment out KAGENT_A2A_URL above to use this approach
# ============================================================================

# KAGENT_BASE_URL=http://localhost:8083
# KAGENT_NAMESPACE=kagent
# KAGENT_AGENT_NAME=k8s-agent

# ============================================================================
# Multi-Cluster Routing Configuration (FEATURE FLAG)
# ============================================================================
# RECOMMENDED: Run this bot on your laptop or a central server.
#
# Single @kagent bot that routes to multiple agents/clusters based on
# keywords detected in user messages.

# Enable multi-cluster routing mode
ENABLE_MULTI_CLUSTER=true

# Base configuration (needed for pattern-based routing)
KAGENT_BASE_URL=http://kagent-controller:8083
KAGENT_NAMESPACE=kagent

# Comma-separated list of cluster names
# These names will be detected in user messages
KAGENT_CLUSTERS=test,dev,prod

# Default cluster when no cluster keyword is detected
KAGENT_DEFAULT_CLUSTER=test

# ============================================================================
# Configuration Method 1: Pattern-Based (RECOMMENDED for your setup)
# ============================================================================
# Use when you have ONE Kagent controller with multiple agents,
# each agent configured to talk to a different cluster.
#
# Example agents:
# - k8s-agent-test → queries test cluster
# - k8s-agent-dev  → queries dev cluster
# - k8s-agent-prod → queries prod cluster

# Agent name pattern with {cluster} placeholder
KAGENT_AGENT_PATTERN=k8s-agent-{cluster}

# This generates endpoints like:
# - test: http://kagent-controller:8083/api/a2a/kagent/k8s-agent-test
# - dev:  http://kagent-controller:8083/api/a2a/kagent/k8s-agent-dev
# - prod: http://kagent-controller:8083/api/a2a/kagent/k8s-agent-prod

# ============================================================================
# Configuration Method 2: Full URLs Per Cluster (Alternative)
# ============================================================================
# Use when each cluster has its own separate Kagent controller.
# Comment out KAGENT_AGENT_PATTERN and set these instead:

# KAGENT_TEST_URL=http://test-kagent.example.com:8083/api/a2a/kagent/k8s-agent
# KAGENT_DEV_URL=http://dev-kagent.example.com:8083/api/a2a/kagent/k8s-agent
# KAGENT_PROD_URL=http://prod-kagent.example.com:8083/api/a2a/kagent/k8s-agent

# ============================================================================
# How Multi-Cluster Routing Works:
#
# 1. Run this bot on your laptop/server (ONE instance)
# 2. User says: "@kagent list pods in test cluster"
# 3. Bot detects "test" keyword
# 4. Routes to: http://kagent-controller:8083/api/a2a/kagent/k8s-agent-test
# 5. Maintains separate conversation context per cluster per thread
#
# Deployment:
# - Local: Use kubectl port-forward or VPN to reach Kagent controller
# - Server: Ensure server can reach the Kagent controller
#
# Supported cluster keyword aliases:
# - "production" → "prod"
# - "development" → "dev"
# - "testing" → "test"
# - "staging" → "stage"
# ============================================================================

# ============================================================================
# Default values from Kagent quickstart:
# - Namespace: kagent
# - Agent: k8s-agent
# - Port: 8083
# ============================================================================